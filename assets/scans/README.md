# Scan assets

Large scan assets are not committed by default.

Use:

```text
assets/scans/source/   raw downloaded scans
assets/scans/reduced/  optimized .glb files
```

The game is playable without these files.
